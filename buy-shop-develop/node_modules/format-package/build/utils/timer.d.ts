import * as convertHrtime from 'convert-hrtime';
declare type HrtimeResponse = [number, number];
export default class Timer {
    private startTime;
    start(): HrtimeResponse;
    end(): convertHrtime.HRTime;
}
export {};
