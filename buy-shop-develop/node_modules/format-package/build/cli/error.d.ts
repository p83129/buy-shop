export declare type LogError = (Error & {
    stderr?: string;
    stdout?: string;
    code?: number;
}) | {
    stderr?: string;
    stdout?: string;
    code?: number;
};
export default function logErrorAndExit(error?: LogError): void;
